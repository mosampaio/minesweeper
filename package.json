{
  "name": "application-name",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app"
  },
  "dependencies": {
    "express": ">=0",
    "socket.io": ">=0",
    "mongoskin": ">=0",
    "connect-mongo": ">=0",
    "cookie": ">=0",
    "connect" : ">=0"
    
  }
}